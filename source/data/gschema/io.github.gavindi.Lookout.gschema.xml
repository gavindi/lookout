<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <enum id="io.github.gavindi.Lookout.SortKey">
    <value nick="date" value="0"/>
    <value nick="sender" value="1"/>
    <value nick="subject" value="2"/>
  </enum>

  <schema id="io.github.gavindi.Lookout" path="/io/github/gavindi/Lookout/">

    <!-- General -->
    <key name="start-at-login" type="b">
      <default>false</default>
      <summary>Start Lookout at login</summary>
      <description>Start Lookout hidden when you log in, so mail and calendar notifications keep working without a window.</description>
    </key>
    <key name="close-to-background" type="b">
      <default>true</default>
      <summary>Keep running when the window is closed</summary>
      <description>Continue syncing and showing mail and calendar notifications after the window is closed; quit from the File menu.</description>
    </key>

    <!-- Appearance -->
    <key name="animate-transitions" type="b">
      <default>true</default>
      <summary>Animate transitions</summary>
      <description>Fade between views when switching messages.</description>
    </key>
    <key name="window-background-path" type="s">
      <default>''</default>
      <summary>Custom window background image</summary>
      <description>Path to a user-picked image used as the window background; empty means the bundled artwork.</description>
    </key>
    <key name="background-brightness" type="d">
      <range min="0" max="1"/>
      <default>0.75</default>
      <summary>Window background brightness</summary>
      <description>How bright the window background is shown - the bundled artwork as well as a user-picked image: 1.0 means no darkening, 0.0 means darkened to black.</description>
    </key>
    <key name="theme-id" type="s">
      <default>'flat-dark'</default>
      <summary>Colour theme</summary>
      <description>The bundled flat-token theme: 'system' follows the system light/dark scheme, 'flat-dark' and 'flat-light' pin the app's surfaces to one scheme.</description>
    </key>
    <key name="accent-color" type="s">
      <default>''</default>
      <summary>Accent colour</summary>
      <description>A CSS colour string (as written by gdk::RGBA::to_str) used app-wide instead of the system accent; empty means follow the system accent.</description>
    </key>

    <!-- View → Layout -->
    <key name="layout-folder-pane" type="b">
      <default>true</default>
      <summary>Show the folder pane</summary>
    </key>
    <key name="layout-reading-pane" type="b">
      <default>true</default>
      <summary>Show the reading pane</summary>
    </key>
    <key name="layout-calendar-overview" type="b">
      <default>true</default>
      <summary>Show the calendar overview</summary>
    </key>
    <key name="layout-vertical-spacing" type="s">
      <default>'medium'</default>
      <summary>Vertical spacing in the folder and message list panes</summary>
      <description>Vertical spacing between items in the folder and message list panes: tight (the least padding), medium (5% more padding) or loose (10% more padding). Medium is the default.</description>
    </key>
    <key name="pane-folder-width-percent" type="d">
      <default>-1</default>
      <summary>Folder pane width as a percentage of the window width</summary>
      <description>Last width the folder pane was dragged to, as a percentage of the application window width; -1 means never set. Reapplied when the window is resized.</description>
    </key>
    <key name="pane-message-list-width-percent" type="d">
      <default>-1</default>
      <summary>Message list pane width as a percentage of the window width</summary>
      <description>Last width the message list pane was dragged to, as a percentage of the application window width; -1 means never set. Reapplied when the window is resized.</description>
    </key>
    <key name="pane-calendar-sidebar-width-percent" type="d">
      <default>-1</default>
      <summary>Calendar sidebar pane width as a percentage of the window width</summary>
      <description>Last width the calendar sidebar pane was dragged to, as a percentage of the application window width; -1 means never set. Reapplied when the window is resized.</description>
    </key>
    <key name="pane-contacts-sidebar-width-percent" type="d">
      <default>-1</default>
      <summary>Contacts sidebar pane width as a percentage of the window width</summary>
      <description>Last width the contacts sidebar pane was dragged to, as a percentage of the application window width; -1 means never set. Reapplied when the window is resized.</description>
    </key>
    <key name="pane-config-sidebar-width-percent" type="d">
      <default>-1</default>
      <summary>Config sidebar pane width as a percentage of the window width</summary>
      <description>Last width the Config sidebar pane was dragged to, as a percentage of the application window width; -1 means never set. Reapplied when the window is resized.</description>
    </key>

    <!-- Mail list -->
    <key name="sort-key" enum="io.github.gavindi.Lookout.SortKey">
      <default>'date'</default>
      <summary>Message list sort key</summary>
    </key>
    <key name="sort-descending" type="b">
      <default>true</default>
      <summary>Sort newest first</summary>
    </key>
    <key name="mail-favorites" type="as">
      <default>[]</default>
      <summary>Favorite mailboxes</summary>
      <description>MailboxIds of folders pinned to the top of the folder tree.</description>
    </key>
    <key name="accounts-disabled" type="as">
      <default>[]</default>
      <summary>Disabled GNOME Online Accounts accounts</summary>
      <description>AccountIds of GOA accounts whose mail, calendars, tasks and contacts Lookout must not process; everything else is enabled by default.</description>
    </key>
    <key name="mail-threaded" type="b">
      <default>true</default>
      <summary>Group replies into conversations</summary>
      <description>Show replies as collapsible conversation threads in the message list instead of bare messages.</description>
    </key>

    <!-- Mail settings -->
    <key name="mail-load-remote-images" type="b">
      <default>false</default>
      <summary>Load images from the web</summary>
      <description>Allow the reading pane to load image resources from remote servers.</description>
    </key>
    <key name="mail-rich-text-default" type="b">
      <default>true</default>
      <summary>Start new messages in the formatted editor</summary>
    </key>
    <key name="mail-message-theme-dark" type="b">
      <default>false</default>
      <summary>Open HTML messages in dark mode</summary>
      <description>When on, newly-opened HTML messages show with their own background removed and colours inverted instead of in their original light form. The reading pane's per-message "Switch message theme" toggle overrides this for the current message either way.</description>
    </key>
    <key name="mail-send-read-receipts" type="b">
      <default>false</default>
      <summary>Send read receipts automatically</summary>
      <description>When a message asks for a read receipt (a Disposition-Notification-To header), reply with one automatically on display. When off, the reading pane asks first.</description>
    </key>
    <key name="mail-notifications-enabled" type="b">
      <default>true</default>
      <summary>Mail notifications</summary>
      <description>Show a desktop notification when new mail arrives or a send fails.</description>
    </key>
    <key name="dock-badge-enabled" type="b">
      <default>true</default>
      <summary>Dock badge</summary>
      <description>Show the total unread count on the app icon in the dock (Ubuntu's Unity LauncherEntry badge).</description>
    </key>

    <!-- Calendar -->
    <key name="calendar-alerts-enabled" type="b">
      <default>true</default>
      <summary>Event alerts</summary>
      <description>Show a notification when an event reminder comes due.</description>
    </key>

    <!-- Keyboard shortcuts -->
    <key name="shortcuts" type="as">
      <default>[]</default>
      <summary>Global keyboard shortcuts</summary>
      <description>One 'action=accelerator' entry per action overriding the built-in default, e.g. 'compose=&lt;Primary&gt;n'. An empty array means all built-in defaults.</description>
    </key>

    <!-- Session memory -->
    <key name="last-view-unified" type="b">
      <default>false</default>
      <summary>Folder pane was on the unified view</summary>
    </key>
    <key name="last-view-mailbox" type="s">
      <default>''</default>
      <summary>Last-open mailbox</summary>
      <description>The MailboxId of the folder the pane reopened on; empty means none.</description>
    </key>

  </schema>
</schemalist>
